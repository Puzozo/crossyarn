# crossyarn-import recognition service — CPU only, no GPU.
fastapi==0.115.*
uvicorn[standard]==0.32.*
numpy==2.*
opencv-python-headless==4.10.*
pillow==11.*
onnxruntime==1.20.*        # CPU inference for the symbol classifier (phase 2)
pytesseract==0.3.*         # legend OCR (requires the tesseract binary on the host)
httpx==0.27.*              # callback to the Next.js internal endpoint
